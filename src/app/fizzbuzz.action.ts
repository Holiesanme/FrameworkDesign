import { createAction } from '@ngrx/store';

export const fizz = createAction('[Counter Component] Fizz');
export const buzz = createAction('[Counter Component] Buzz');
export const reset = createAction('[Counter Component] Reset');